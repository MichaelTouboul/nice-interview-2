const version1 = 'v1'
const dishesRoot = 'dishes'
const ordersRoot = 'orders'
const billsRoot = 'bills'

const routesV1 = {
    version: version1,
    dishes: {
        root: dishesRoot,
    },
    orders: {
        root: ordersRoot,
    },
    bills: {
        root: billsRoot,
    },
}

export default routesV1
